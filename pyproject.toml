[project]
name = "flyrank-fastapi"
version = "0.2.0"
description = "FlyRank CRUD API Module"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "black>=26.5.1",
    "fastapi[standard]>=0.141.1",
    "sqlmodel>=0.0.39",
]
